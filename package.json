{
    "name": "canvas-browser",
    "scripts": {
        "serve": "nodemon server.js --watch server.js --watch _server/",
        "build-jsdom": "browserify -r jsdom -o jsdom.js"
    },
    "dependencies": {
        "byline": "^5.0.0",
        "express": "^4.16.4",
        "jsdom": "^14.0.0"
    },
    "devDependencies": {
        "browserify": "^16.2.3",
        "nodemon": "^1.18.10"
    }
}
